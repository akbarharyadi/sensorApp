<%= form_with(model: measurement, local: true) do |form| %>
  <% if measurement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(measurement.errors.count, "error") %> prohibited this measurement from being saved:</h2>

      <ul>
      <% measurement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :value_temp %>
    <%= form.text_field :value_temp, id: :measurement_value_temp %>
  </div>

  <div class="field">
    <%= form.label :value_hum %>
    <%= form.text_field :value_hum, id: :measurement_value_hum %>
  </div>

  <div class="field">
    <%= form.label :device_id %>
    <%= form.text_field :device_id, id: :measurement_device_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
